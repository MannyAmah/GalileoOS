# Claude Code session state
.claude/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Env
.env
.env.local
.env.*.local
*.pem
*.key

# Stage 0 local dev keypair — explicit alongside the *.pem rule above
# so the directory itself never accumulates other artifacts.
kernel/auth/dev-keys/

# Language artifacts (early; will be refined per service in Week 1)
node_modules/
dist/
build/
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
target/
go.sum.tmp
