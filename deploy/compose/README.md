# `deploy/compose/` — Stage 0 single-VM docker-compose stack

Empty in Stage 0 PR #3. Week 3 lands the docker-compose.yml from `docs/galileo_os_infrastructure_plan.md` Appendix B (Postgres/Supabase, Temporal, NATS JetStream, LiteLLM, Opik, Ollama, plus `galileo-gateway` and `galileo-agent-runner`).
