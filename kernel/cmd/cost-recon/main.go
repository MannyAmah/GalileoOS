// Package main is the cost-meter ↔ Stripe reconciliation script.
//
// Stage 0 skeleton. Full behavior spec'd in docs/plans/STAGE_0_PLAN.md
// §Week 3 deliverable 6: integer-cents comparison, [window_start,
// window_end) RFC 3339 window, non-zero exit on any per-row mismatch.
package main

import "fmt"

func main() {
	fmt.Println("galileo-cost-recon stage0 skeleton")
}
